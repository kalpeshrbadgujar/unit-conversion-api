namespace UnitConversion.Domain.Enums;

/// <summary>
/// Families of units that share the same conversion rules.
/// </summary>
public enum ConversionCategory
{
    Length = 1,
    Weight = 2,
    Temperature = 3,
    Volume = 4,
}
